{
  "name": "@jamjar/database",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "build": "prisma generate",
    "format": "prisma format --schema schema.prisma",
    "migrate": "prisma migrate dev --schema schema.prisma",
    "seed": "npm run migrate && ts-node seeds/basicJam.ts",
    "seed:basic": "npm run migrate && ts-node seeds/basicJam.ts",
    "dev": "concurrently npm:prettier:watch",
    "prettier:watch": "onchange -d 10000 \"**/*.ts\" -- prettier --write --ignore-unknown {{changed}}"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.5",
    "concurrently": "^8.2.2",
    "onchange": "^7.1.0",
    "ts-node": "^10.9.2",
    "tsup": "^8.0.1"
  }
}
